import { HighlightDirective2 } from './highlight.directive';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDirective2();
    expect(directive).toBeTruthy();
  });
});
